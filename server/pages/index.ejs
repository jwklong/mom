<link rel="stylesheet" href="/static/style.css">

<% for (let user of data.players.sort((a, b) => b.height - a.height)) { %>
    <%= user.name %>'s tower is <%= user.wogc.height.toFixed(1) %> meters tall <br>
<% } %>